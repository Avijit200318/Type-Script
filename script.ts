// Enums
// enums help us to group things
enum direction {
    bottom = "bottom",
    left = "left",
    right = 'right',
    top = "top"
}

direction.top;
// then we get this values. its like the object but instead of : here = used.